# Leave unset to use the Vite dev proxy (localhost:8000) or same origin in production.
# Set to your live API to run the local frontend against a remote backend, e.g.:
# VITE_API_BASE_URL=https://sanctions-check.co.uk
